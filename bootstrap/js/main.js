$(document).ready(function() {

    // $('.angel-lightbox > .thumbnail').click(
    //     function (event) {
    //         var targetID = event.target.id;
    //         var index = parseInt(event.target.id.substring("angel-lightbox-".length));
    //         if (!isNaN(index)) {
    //             $('.carousel').carousel(index-1);
    //         }
    //     }
    // );

    $('.carousel').carousel();
});
